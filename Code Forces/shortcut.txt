// some technique that are useful 
string:
    reverse(str.begin(),str.end());
    to_string(int/ll/double/float);
bitset:
    b.to_ulong() // convert it into usigned long
    b.to_string() // convert it into string
float/ Double:
    ceil((double) x / y)
    setprecision(0)     (int) to avoid scientefic notation